# Python dependencies for the simulation and visualization scripts.
#   pip install -r requirements.txt
#
# tkinter (used by python/gui_viewer.py) is part of the Python standard
# library and is not listed here. On some Linux distributions it must be
# installed separately, e.g.  sudo apt install python3-tk
numpy>=1.21
scipy>=1.7
matplotlib>=3.4
